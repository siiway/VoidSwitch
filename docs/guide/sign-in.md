# 登录

VoidSwitch 使用 **Prism**（OAuth / OpenID Connect）进行控制台登录。无需管理本地密码。

## 如何登录

1. 在浏览器中打开控制台 URL。
2. 点击**通过 Prism 登录**。
3. 如提示，在 Prism 中授权应用。
4. 你将重定向回来并进入**控制台**。

## 谁可以进入

通过 Prism 登录是必要的，但并非总是足够的 — 你还需要一个在平台上存在的理由。如果满足以下**任一**条件，你将被允许进入：

- 你是平台主团队的 owner / co-owner / admin（*moderator*）；
- 你的 Prism 团队成员资格映射到至少一个[身份组](/admin/role-groups)；
- 管理员已明确添加你，或者你是第一个用户（将成为 owner）。

如果都不满足，登录将被拒绝，显示**访问被拒绝**消息。请联系管理员为你授予角色或映射你的团队职位。

## 会话

- 你的会话是存储在浏览器中的已签名令牌。它会在配置的时间后过期；只需重新登录即可。
- 如果管理员禁用了你的账户，你的所有会话将立即失效，你的 Void-Token 将停止工作，直到你被重新启用并再次登录。
- 使用侧边栏底部的**退出登录**来结束会话。

## 紧急登录 token

owner、co-owner 和 admin 可以在**设置 → 个人设置**中生成一个个人登录 token。它用于 Prism / OAuth 暂时不可用时进入控制台：

- 登录页底部点击**使用登录 token**，输入 token 即可登录；
- 每个用户只有一个登录 token，轮换后旧 token 立即失效；
- token 只在生成或轮换时显示一次，之后只能看到安全指纹，无法查看明文；
- 普通成员不能生成或使用登录 token，避免绕过身份组评估。

## 文档站点

本文档是**公开的**，托管在独立域名上（无需登录即可阅读）。控制台侧边栏的**文档**选项卡
会在新标签页打开它。文档提供中文与 English，用页面右上角的语言菜单切换。

下一步：[控制台概览](/guide/dashboard)。
